let hatPrice : number  = 100;
console.log(`hat price is ${hatPrice}`);
let bootsPrice : number = 100;
console.log(`boots price is ${bootsPrice}`);

if(hatPrice === bootsPrice)  
    console.log('both are same');
else
    console.log('both are NOT same');

let totalPrice = hatPrice + bootsPrice;
console.log(`The total price is ${totalPrice} and type of totalPrice is ${typeof(totalPrice)}`);



console.log(`The type of null is ${typeof(null)}`);
 